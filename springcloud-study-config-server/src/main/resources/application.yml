spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/bookwed/springcloud-study-config-repo.git
          search-paths: client1
          username: test
          password: test

server:
  port: 9020

